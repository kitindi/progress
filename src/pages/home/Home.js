import React from "react";
import Auth from "../auth/Auth";

const Home = () => {
  return (
    <section className="w-full bg-gray-100 min-h-screen">
      <Auth />
    </section>
  );
};

export default Home;
